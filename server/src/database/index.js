const UserModel = require('./models/user.schema');
const LogModel = require('./models/log.schema');
const AccessTokenModel = require('./models/access-token.schema');

module.exports = {
    UserModel,
    LogModel,
    AccessTokenModel
};
